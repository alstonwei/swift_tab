# swift_tab
swift tap

```swift

        qytabBar = QYTabBar.init()
        qytabBar?.delegate = self
        qytabBar?.backgroundColor = UIColor.green
        qytabBar?.frame = CGRect.init(origin: CGPoint.init(x: 0, y: tabbarHeight - controlHeight),size: CGSize.init(width: parrentWidth, height: 44));
```
